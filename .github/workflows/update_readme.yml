name: Update README from Website

on:
  schedule:
    - cron: '*/5 * * * *'  # Runs every 5 minutes
  push:
    branches:
      - main  # Or your default branch

jobs:
  update-readme:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.x'

    - name: Install dependencies
      run: |
        pip install requests

    - name: Update README
      env:
        GH_TOKEN: ${{ secrets.GH_TOKEN }}
      run: python update_readme.py

    - name: Commit and push if changes
      env:
        GH_TOKEN: ${{ secrets.GH_TOKEN }}
      run: |
        git config --global user.email "thivyanth2004@gmail.com"
        git config --global user.name "thivyanth"
        git remote set-url origin https://x-access-token:${{ secrets.GH_TOKEN }}@github.com/${{ github.repository }}
        git diff --exit-code || (git add . && git commit -m "Update README from website" && git push origin main)
